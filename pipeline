pipeline
	{
	 agent any
		stages {
			stage ("BUild") {
					steps {
						echo "this is build"
						sh "git pull git@github.com:BharathSukka/seven.git"
						sh "ls"
						}
					}
			stage ("Deploy") {
					steps {
						echo "this is for deploy"
						}
					}
			}	
	}

